export async function deleteLeaveRequest(id: number): Promise<{ success: boolean }> {
    // This is a placeholder declaration! Real code should be implemented here.
    // The goal of this handler is deleting leave requests (employee can delete pending requests only).
    // Should validate permissions, request existence, and ensure only pending requests can be deleted.
    return Promise.resolve({ success: true });
}